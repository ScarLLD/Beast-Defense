using DG.Tweening;
using UnityEngine;
using UnityEngine.EventSystems;

public class SkinItemUIAnimator : MonoBehaviour
{
    //[SerializeField] private RectTransform _rectTransform;

    //private Vector3 _animTargetScale;
    //private float _animDuration = 1f;

    private void Awake()
    {
        //_animTargetScale = _rectTransform.localScale * 0.95f;
    }

    //public void OnPointerClick(PointerEventData eventData)
    //{
    //    //_rectTransform.DOScale(_animTargetScale, _animDuration).SetEase(Ease.OutBack);
    //}
}
